'use strict';

/**
 * sliderealisation controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::sliderealisation.sliderealisation');
