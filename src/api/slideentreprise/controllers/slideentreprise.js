'use strict';

/**
 * slideentreprise controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::slideentreprise.slideentreprise');
